## 1. 面板分区重排

- [x] 1.1 重构 `MenuBarView` 的主内容结构，将当前账户摘要、中部切换列表、底部操作区拆成清晰的三段式布局
- [x] 1.2 在三段之间加入稳定的分隔和间距规则，保证窄宽度下仍保持参考图那样的层级感
- [x] 1.3 在 `AppState` 中引入按账号缓存结构，保存每个账号独立的摘要与用量快照
- [x] 1.4 提供当前账号刷新和单账号刷新 API，确保刷新目标只作用于对应账号

## 2. 当前账户摘要

- [x] 2.1 调整 `CurrentAccountSection`，让顶部显示应用标题、当前邮箱、计划类型和更新时间
- [x] 2.2 将当前账户用量区整理为更强的摘要卡样式，保留 5 Hours / Weekly 的分区与重置文本
- [x] 2.3 确保当前账户区域在只有单个用量窗口时自动扩展为全宽
- [x] 2.4 为当前账户摘要区加入独立刷新入口，并接到当前账号局部刷新逻辑

## 3. 账户切换列表

- [x] 3.1 重构 `SavedAccountRow`，将邮箱、计划类型和用量摘要组织成更紧凑的行式结构
- [x] 3.2 确保账户列表在只有单个用量窗口时使用全宽布局，在双窗口时保持可读的并列布局
- [x] 3.3 校准点击区域与文本截断，让长邮箱在窄面板中仍可快速辨认
- [x] 3.4 为每个账户行增加独立刷新入口，并确保刷新时只更新目标账号缓存
- [x] 3.5 引入按账号缓存回显，让面板打开时优先展示缓存数据，再后台补刷新

## 4. 底部动作区

- [x] 4.1 将底部操作入口整理为纵向动作列表，统一图标 + 文案样式
- [x] 4.2 根据设置状态动态展示 Show Emails / Mask Emails 入口文案

## 5. 验证与收尾

- [x] 5.1 运行 `swift build` 并确认无新的编译警告
- [ ] 5.2 在菜单栏面板中验证单账号、多个账号、长邮箱、只有 primary 用量窗口等场景
- [ ] 5.3 对照参考图检查分区顺序、视觉层级和底部动作区是否一致
- [ ] 5.4 验证单账号刷新只影响当前行，不会让其他账号列表项闪烁或重载
- [ ] 5.5 验证当前账号刷新只更新摘要区缓存，不触发保存列表整体重载
